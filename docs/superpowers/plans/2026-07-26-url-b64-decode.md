# url-b64-decode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一个 Go CLI 工具：GET 请求指定 URL，将纯 base64 响应体解码为 UTF-8 文本打印到 stdout，支持超时与重试。

**Architecture:** 单模块单文件 `main.go`。两个可测试的核心函数：`decodeBase64UTF8`（base64 解码 + UTF-8 校验）和 `fetch`（带重试的 GET）。`main` 只做参数解析和串接。测试全部放在 `main_test.go`。

**Tech Stack:** Go（仅标准库：`flag`、`net/http`、`net/http/httptest`、`encoding/base64`、`unicode/utf8`、`io`、`strings`、`time`、`os`、`fmt`）。

## Global Constraints

- 仅使用 Go 标准库，禁止引入第三方依赖
- 模块名：`url-b64-decode`
- 成功时 stdout 只输出解码结果；所有错误信息走 stderr，退出码非 0（参数错误退出码 2，运行错误退出码 1）
- base64 解码顺序：先 `base64.StdEncoding`，失败再试 `base64.URLEncoding`
- 4xx 不重试；网络错误、超时、5xx 才重试
- 设计文档：`docs/superpowers/specs/2026-07-26-url-b64-decode-design.md`

---

### Task 1: 环境准备 + Go 模块初始化

**Files:**
- Create: `go.mod`（由 `go mod init` 生成）
- Create: `.gitignore`

**Interfaces:**
- Consumes: 无
- Produces: 可用的 Go 工具链、模块 `url-b64-decode`，供后续所有任务使用

- [ ] **Step 1: 安装 Go（本机尚未安装，Homebrew 可用）**

```bash
brew install go
```

- [ ] **Step 2: 验证安装**

Run: `go version`
Expected: 输出 `go version go1.x ...`

- [ ] **Step 3: 初始化模块**

在项目根目录 `/Users/wiki/work/project/url-base64-decode` 执行：

```bash
go mod init url-b64-decode
```

- [ ] **Step 4: 创建 .gitignore**

创建 `.gitignore`，内容：

```
url-b64-decode
```

（忽略 `go build` 产出的二进制文件）

- [ ] **Step 5: Commit**

```bash
git add go.mod .gitignore
git commit -m "chore: init go module"
```

---

### Task 2: decodeBase64UTF8 函数（TDD）

**Files:**
- Create: `main.go`
- Test: `main_test.go`

**Interfaces:**
- Consumes: 无
- Produces: `func decodeBase64UTF8(raw string) (string, error)` — 输入原始响应体字符串（可含首尾空白），返回解码后的 UTF-8 文本；base64 无效或结果非 UTF-8 时返回 error。Task 4 的 `main` 会调用它。

- [ ] **Step 1: 写失败的表驱动测试**

创建 `main_test.go`：

```go
package main

import "testing"

func TestDecodeBase64UTF8(t *testing.T) {
	tests := []struct {
		name    string
		input   string
		want    string
		wantErr bool
	}{
		{"standard base64", "aGVsbG8gd29ybGQ=", "hello world", false},
		{"url-safe base64", "PDw_Pz4-", "<<??>>", false},
		{"utf8 chinese", "5L2g5aW9", "你好", false},
		{"surrounding whitespace", "  aGVsbG8=\n", "hello", false},
		{"invalid base64", "!!!not base64!!!", "", true},
		{"decoded not utf8", "gIE=", "", true},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, err := decodeBase64UTF8(tt.input)
			if (err != nil) != tt.wantErr {
				t.Fatalf("error = %v, wantErr = %v", err, tt.wantErr)
			}
			if got != tt.want {
				t.Fatalf("got %q, want %q", got, tt.want)
			}
		})
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./... -run TestDecodeBase64UTF8 -v`
Expected: 编译失败，`undefined: decodeBase64UTF8`

- [ ] **Step 3: 写最小实现**

创建 `main.go`：

```go
package main

import (
	"encoding/base64"
	"fmt"
	"strings"
	"unicode/utf8"
)

// decodeBase64UTF8 trims surrounding whitespace, decodes standard or
// URL-safe base64, and verifies the result is valid UTF-8.
func decodeBase64UTF8(raw string) (string, error) {
	s := strings.TrimSpace(raw)
	data, err := base64.StdEncoding.DecodeString(s)
	if err != nil {
		var urlErr error
		data, urlErr = base64.URLEncoding.DecodeString(s)
		if urlErr != nil {
			return "", fmt.Errorf("invalid base64 (input starts with %q): %v", head(s, 100), err)
		}
	}
	if !utf8.Valid(data) {
		return "", fmt.Errorf("decoded content is not valid UTF-8 (input starts with %q)", head(s, 100))
	}
	return string(data), nil
}

// head returns at most the first n bytes of s, for error messages.
func head(s string, n int) string {
	if len(s) > n {
		return s[:n]
	}
	return s
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `go test ./... -run TestDecodeBase64UTF8 -v`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add main.go main_test.go
git commit -m "feat: add base64 decode with UTF-8 validation"
```

---

### Task 3: fetch 函数——带重试的 GET（TDD）

**Files:**
- Modify: `main.go`（追加函数）
- Test: `main_test.go`（追加测试）

**Interfaces:**
- Consumes: 无（与 Task 2 并列）
- Produces: `func fetch(client *http.Client, url string, retries int, retryWait time.Duration) (string, error)` — 发起 GET，2xx 返回响应体字符串；网络错误/超时/5xx 最多重试 `retries` 次（总尝试次数 = retries+1），每次间隔 `retryWait`；4xx 立即返回 error 不重试。Task 4 的 `main` 会调用它。

- [ ] **Step 1: 写失败的测试（httptest 覆盖四个场景）**

在 `main_test.go` 追加：

```go
func TestFetch(t *testing.T) {
	t.Run("success returns body", func(t *testing.T) {
		srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
			fmt.Fprint(w, "aGVsbG8=")
		}))
		defer srv.Close()
		got, err := fetch(srv.Client(), srv.URL, 3, 0)
		if err != nil {
			t.Fatalf("unexpected error: %v", err)
		}
		if got != "aGVsbG8=" {
			t.Fatalf("got %q, want %q", got, "aGVsbG8=")
		}
	})

	t.Run("retries on 5xx then succeeds", func(t *testing.T) {
		var calls int32
		srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
			if atomic.AddInt32(&calls, 1) <= 2 {
				w.WriteHeader(http.StatusInternalServerError)
				return
			}
			fmt.Fprint(w, "b2s=")
		}))
		defer srv.Close()
		got, err := fetch(srv.Client(), srv.URL, 3, 0)
		if err != nil {
			t.Fatalf("unexpected error: %v", err)
		}
		if got != "b2s=" {
			t.Fatalf("got %q, want %q", got, "b2s=")
		}
		if n := atomic.LoadInt32(&calls); n != 3 {
			t.Fatalf("server called %d times, want 3", n)
		}
	})

	t.Run("4xx fails without retry", func(t *testing.T) {
		var calls int32
		srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
			atomic.AddInt32(&calls, 1)
			w.WriteHeader(http.StatusNotFound)
		}))
		defer srv.Close()
		_, err := fetch(srv.Client(), srv.URL, 3, 0)
		if err == nil {
			t.Fatal("expected error, got nil")
		}
		if n := atomic.LoadInt32(&calls); n != 1 {
			t.Fatalf("server called %d times, want 1", n)
		}
	})

	t.Run("exhausted retries returns error", func(t *testing.T) {
		var calls int32
		srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
			atomic.AddInt32(&calls, 1)
			w.WriteHeader(http.StatusInternalServerError)
		}))
		defer srv.Close()
		_, err := fetch(srv.Client(), srv.URL, 2, 0)
		if err == nil {
			t.Fatal("expected error, got nil")
		}
		if n := atomic.LoadInt32(&calls); n != 3 {
			t.Fatalf("server called %d times, want 3", n)
		}
	})

	t.Run("timeout is retried then fails", func(t *testing.T) {
		var calls int32
		srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
			atomic.AddInt32(&calls, 1)
			time.Sleep(200 * time.Millisecond)
		}))
		defer srv.Close()
		client := &http.Client{Timeout: 50 * time.Millisecond}
		_, err := fetch(client, srv.URL, 1, 0)
		if err == nil {
			t.Fatal("expected error, got nil")
		}
		if n := atomic.LoadInt32(&calls); n != 2 {
			t.Fatalf("server called %d times, want 2", n)
		}
	})
}
```

同时把 `main_test.go` 的 import 块改为：

```go
import (
	"fmt"
	"net/http"
	"net/http/httptest"
	"sync/atomic"
	"testing"
	"time"
)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./... -run TestFetch -v`
Expected: 编译失败，`undefined: fetch`

- [ ] **Step 3: 写最小实现**

在 `main.go` 追加（import 块补充 `"io"`、`"net/http"`、`"time"`）：

```go
// fetch GETs url, returning the response body on any 2xx status.
// Network errors, timeouts, and 5xx responses are retried up to
// `retries` times with `retryWait` between attempts; 4xx fails immediately.
func fetch(client *http.Client, url string, retries int, retryWait time.Duration) (string, error) {
	var lastErr error
	for attempt := 0; attempt <= retries; attempt++ {
		if attempt > 0 {
			time.Sleep(retryWait)
		}
		body, retryable, err := doGet(client, url)
		if err == nil {
			return body, nil
		}
		if !retryable {
			return "", err
		}
		lastErr = err
	}
	return "", fmt.Errorf("all %d attempts failed, last error: %v", retries+1, lastErr)
}

func doGet(client *http.Client, url string) (body string, retryable bool, err error) {
	resp, err := client.Get(url)
	if err != nil {
		return "", true, err
	}
	defer resp.Body.Close()
	data, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", true, err
	}
	switch {
	case resp.StatusCode >= 200 && resp.StatusCode < 300:
		return string(data), false, nil
	case resp.StatusCode >= 500:
		return "", true, fmt.Errorf("server error: HTTP %d", resp.StatusCode)
	default:
		return "", false, fmt.Errorf("HTTP %d", resp.StatusCode)
	}
}
```

- [ ] **Step 4: 运行全部测试确认通过**

Run: `go test ./... -v`
Expected: 全部 PASS（含 Task 2 的测试）

- [ ] **Step 5: Commit**

```bash
git add main.go main_test.go
git commit -m "feat: add fetch with timeout-aware retry"
```

---

### Task 4: main 入口——CLI 参数与串接

**Files:**
- Modify: `main.go`（追加 `main` 函数）

**Interfaces:**
- Consumes: `decodeBase64UTF8(raw string) (string, error)`（Task 2）、`fetch(client *http.Client, url string, retries int, retryWait time.Duration) (string, error)`（Task 3）
- Produces: 可执行命令 `url-b64-decode [flags] <URL>`，flags 为 `-timeout`（默认 10s）、`-retries`（默认 3）、`-retry-wait`（默认 1s）

- [ ] **Step 1: 实现 main**

在 `main.go` 追加（import 块补充 `"flag"`、`"os"`）：

```go
func main() {
	timeout := flag.Duration("timeout", 10*time.Second, "per-request timeout")
	retries := flag.Int("retries", 3, "retry count on network errors and 5xx")
	retryWait := flag.Duration("retry-wait", time.Second, "wait between retries")
	flag.Usage = func() {
		fmt.Fprintf(flag.CommandLine.Output(), "Usage: url-b64-decode [flags] <URL>\n\nFlags:\n")
		flag.PrintDefaults()
	}
	flag.Parse()
	if flag.NArg() != 1 {
		flag.Usage()
		os.Exit(2)
	}

	client := &http.Client{Timeout: *timeout}
	body, err := fetch(client, flag.Arg(0), *retries, *retryWait)
	if err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		os.Exit(1)
	}
	decoded, err := decodeBase64UTF8(body)
	if err != nil {
		fmt.Fprintln(os.Stderr, "error:", err)
		os.Exit(1)
	}
	fmt.Println(decoded)
}
```

- [ ] **Step 2: 构建并检查用法输出**

```bash
go build && ./url-b64-decode
```

Expected: 打印 Usage 与三个 flag 说明，退出码 2（用 `echo $?` 确认）

- [ ] **Step 3: 端到端冒烟测试（本地服务器）**

```bash
printf '你好，世界' | base64 > /private/tmp/claude-501/-Users-wiki-work-project-url-base64-decode/0fdfb0d6-a244-485a-a488-9e5fa7d656ce/scratchpad/b64.txt
python3 -m http.server 8765 --directory /private/tmp/claude-501/-Users-wiki-work-project-url-base64-decode/0fdfb0d6-a244-485a-a488-9e5fa7d656ce/scratchpad &
sleep 1
./url-b64-decode http://localhost:8765/b64.txt
kill %1
```

Expected: 打印 `你好，世界`

- [ ] **Step 4: 运行全部测试 + go vet**

Run: `go test ./... && go vet ./...`
Expected: 测试 PASS，vet 无输出

- [ ] **Step 5: Commit**

```bash
git add main.go
git commit -m "feat: add CLI entrypoint with timeout/retry flags"
```
